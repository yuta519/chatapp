<%= form_with(model: @user) do |f| %>
  <%= render 'shared/error_messages', object: @user %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>
  <%= f.submit "Save" %>
<% end %>
